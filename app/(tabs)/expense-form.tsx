import { SafeAreaView, Text } from "react-native";

export default function ExpenseForm() {
  return (
    <SafeAreaView
      style={{ flex: 1, justifyContent: "center", alignItems: "center" }}
    >
      <Text>This is the expense form</Text>
    </SafeAreaView>
  );
}
